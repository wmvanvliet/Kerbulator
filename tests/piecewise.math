x = 2
x ={ (x^2-x) / x, if x ≥ 1
               0, otherwise
x ={ 4, if x ≠ 1
     3, otherwise
x ={ 1.5, if x == 1
     2.5, if x == 2
     3.5, if x == 3
     4.5, otherwise
x ={ 3, if 3 ≤ x < 4
     5, otherwise
